[project]
name = "email-sender-agent"
version = "0.1.0"
description = "Sends travel itineraries via Gmail API"
dependencies = [
    "google-adk>=1.27.4,<2.0.0",
    "httpx>=0.21.0",
]

[tool.uv]
package = true
